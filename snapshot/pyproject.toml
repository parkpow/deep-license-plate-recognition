[project]
name = "ftp-to-snapshot-cloud"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "pyftpdlib",
    "python-dotenv>=1.2.2",
    "requests",
]
