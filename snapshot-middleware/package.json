{
  "name": "snapshot-middleware",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:rollbar": "wrangler deploy -c rollbar.toml",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "logs": "wrangler tail",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "error-stack-parser": "^2.1.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.62",
    "@vitest/coverage-istanbul": "^3.2.4",
    "vitest": "3.2.4",
    "wrangler": "^4.30.0"
  },
  "type": "module"
}
